There is a 'lang' property on the rootScope. remove langManager injections from all controllers and update templates accordingly.
auth module - remove apiClient dependency



/*** Angular 1.3 migration ***/

Remove all 'replace' keys from directives objects
change child state names to dot notation (start.result instead of start-result + 'parent' property)