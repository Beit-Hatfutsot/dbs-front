/**
 * @ngdoc module
 * @name cache
 *
 * @description
 * Module to handle all cache related stuff.
 * We might want to replace it with some of Angular's built-in cache capabilities. 
 */
angular.module('cache', []);